define([
    "utils/CountDownTimer"
], function (CountDownTimer) {
    "use strict";

    describe("기본 기능 테스트", function () {
        it("start", function () {
            var timer = CountDownTimer({

            })
            // countTimer.print();
            // expect(a).toBe(true);
        });
    });
});